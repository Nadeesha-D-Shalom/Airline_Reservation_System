# ✈️ Version 1.9.6 — Complaint Management System

## 🚀 Features Added
- Added **Complaint Management System** for registered users and admins.
- Implemented backend `/api/complaints/**` endpoints (create, update, status control).
- Added `Complaint.java`, `ComplaintRepository`, `ComplaintService`, and `ComplaintController`.
- Created `ComplaintForm.jsx` for users with auto-filled email and booking ID.
- Added `MyComplaintsPage.jsx` showing user complaints with **color-coded statuses** (Pending / In Review / Resolved / Closed).
- Developed `EditComplaintPage.jsx` (editable only when status = Pending).
- Added `AdminComplaintDashboard.jsx` for full admin control and updates.
- Integrated **“Report a Complaint”** button in booking pages.
- Updated `Navbar` with **My Complaints** (User) and **Complaints** (Admin).
- Designed `ComplaintPage.css` for unified airline-style UI.
- Improved backend validation — prevents edits post admin review, ensures real-time sync between admin/user views.

## 🧩 Highlights
Introduced a complete **Complaint Handling Module**, integrating user-side reporting and admin-side resolution workflow with real-time UI updates.

